<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user_book">

	<!-- 검색 시 카테고리 명 입력시 찾을 수 없음. -->
	<select id="tot_size" parameterType="bean.Page" resultType="Integer">
		select count(code) from item
		where codeName like  '%${findStr}%' 
		or writer like '%${findStr}%' 
		or company like     '%${findStr}%' 
		or list like     '%${findStr}%'
		or contents like     '%${findStr}%' 
		or nal like     '%${findStr}%' 
		or price like     '%${findStr}%' 
		or ea like     '%${findStr}%' 
	</select>

	<select id='select' parameterType="bean.Page" resultType="user_book.UserBookVo">
		select * from item
		where code like  '%${findStr}%' 
		or codeName like '%${findStr}%' 
		or writer like '%${findStr}%' 
		or company like     '%${findStr}%' 
		or list like     '%${findStr}%'
		or contents like     '%${findStr}%' 
		or nal like     '%${findStr}%' 
		or price like     '%${findStr}%' 
		or ea like     '%${findStr}%' 
		order by code 
		limit ${startNo}, ${listSize}	
	</select>

</mapper>